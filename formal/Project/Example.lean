import Mathlib

/-!
Example formalization stub — corresponds to no paper result yet. Delete this
file once real theorems are added; keep the module-docstring-per-file
convention: state which `\theorem`/`\proposition`/`\lemma` label a file
formalizes.
-/

example : 1 + 1 = 2 := rfl
